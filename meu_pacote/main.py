from formatador import caixa_alta
from numeros import par

texto = str(input('Digite algo para aparecer em maiúsculo: '))

caixa_alta(texto)

numero = int(input('Digite um número para saber se é par ou ímpar: '))
par(numero)